# دليل تشغيل مشروع OmniMart

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:
- ✅ **PHP 8.1** أو أحدث
- ✅ **Composer** (مدير حزم PHP)
- ✅ **Node.js** و **npm** (للملفات الأمامية)
- ✅ **MySQL/MariaDB** (قاعدة البيانات)

---

## 🚀 خطوات التشغيل السريعة

### الخطوة 1: تشغيل Vite (للملفات الأمامية)

افتح **PowerShell** أو **Terminal** في مجلد `core` وقم بتشغيل:

```bash
cd core
npm run dev
```

**⚠️ مهم:** اترك هذه النافذة مفتوحة ولا تغلقها!

---

### الخطوة 2: تشغيل الخادم (PHP Server)

افتح **PowerShell** جديد في **المجلد الرئيسي** (`omnimart-62nulled`) واختر إحدى الطرق التالية:

#### ✅ الطريقة الأسهل (موصى بها):

انقر نقراً مزدوجاً على أحد الملفات التالية:
- `start-server.ps1` (لـ PowerShell)
- `start-server.bat` (لـ Command Prompt)

#### أو شغّل يدوياً:

```bash
php -S 127.0.0.1:8000 -t . router.php
```

**ملاحظة:** إذا لم يكن `router.php` موجوداً، سيقوم الملف `start-server.ps1` بإنشائه تلقائياً.

---

### الخطوة 3: فتح المشروع في المتصفح

افتح المتصفح واذهب إلى:
- **http://127.0.0.1:8000**
- أو **http://localhost:8000**

---

## 📝 ملاحظات مهمة

### قبل التشغيل:

1. **إعداد قاعدة البيانات:**
   - افتح ملف `.env` في مجلد `core` (أو أنشئه من `.env.example` إذا كان موجوداً)
   - عدّل إعدادات قاعدة البيانات:
     ```env
     DB_CONNECTION=mysql
     DB_HOST=127.0.0.1
     DB_PORT=3306
     DB_DATABASE=اسم_قاعدة_البيانات
     DB_USERNAME=اسم_المستخدم
     DB_PASSWORD=كلمة_المرور
     ```

2. **إنشاء مفتاح التطبيق (إذا لزم الأمر):**
   ```bash
   cd core
   php artisan key:generate
   ```

3. **تشغيل Migrations:**
   ```bash
   cd core
   php artisan migrate
   ```

4. **إنشاء رابط التخزين:**
   ```bash
   cd core
   php artisan storage:link
   ```

---

## ⚠️ نصائح مهمة

- يجب أن يكون لديك **نافذتان Terminal** مفتوحتان:
  1. واحدة لـ `npm run dev` (في مجلد `core`)
  2. واحدة لـ `php -S 127.0.0.1:8000` (في المجلد الرئيسي)

- **لا تغلق هاتين النافذتين** أثناء العمل على المشروع!

- لإيقاف الخوادم: اضغط `Ctrl + C` في كل نافذة

---

## 🔧 استكشاف الأخطاء

### ❌ خطأ: "PHP is not found in PATH"
- تأكد من تثبيت PHP
- أضف PHP إلى متغيرات البيئة (PATH)

### ❌ خطأ: "Port 8000 already in use"
```bash
php -S 127.0.0.1:8001 -t . router.php
```

### ❌ خطأ: "Database connection failed"
- تحقق من إعدادات قاعدة البيانات في `.env`
- تأكد من أن MySQL يعمل
- تأكد من إنشاء قاعدة البيانات

### ❌ خطأ: "Class not found"
```bash
cd core
composer dump-autoload
```

### ❌ الصور لا تظهر
```bash
cd core
php artisan storage:link
```

### ❌ خطأ: "router.php not found"
- الملف `start-server.ps1` سينشئه تلقائياً
- أو شغّل: `php -S 127.0.0.1:8000 -t . router.php`

---

## 📞 المساعدة الإضافية

إذا واجهت مشاكل:
1. تحقق من ملف `core/storage/logs/laravel.log` للأخطاء
2. تحقق من إعدادات قاعدة البيانات في `.env`
3. تأكد من تثبيت جميع المتطلبات (PHP 8.1+, Composer, Node.js)

---

## ✅ ملخص سريع

```bash
# Terminal 1 - في مجلد core
cd core
npm run dev

# Terminal 2 - في المجلد الرئيسي
# انقر نقراً مزدوجاً على start-server.ps1
# أو شغّل:
php -S 127.0.0.1:8000 -t . router.php

# ثم افتح المتصفح على:
# http://127.0.0.1:8000
```

