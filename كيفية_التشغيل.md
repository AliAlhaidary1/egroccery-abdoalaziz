# كيفية تشغيل مشروع OmniMart

## ✅ تم إكمال التثبيت

تم تثبيت جميع المكتبات المطلوبة:
- ✅ مكتبات PHP (Composer)
- ✅ مكتبات JavaScript (npm)
- ✅ مفتاح التطبيق

## 🚀 خطوات التشغيل

### الطريقة 1: استخدام Laravel Development Server

#### الخطوة 1: تشغيل Vite (للملفات الأمامية)

افتح PowerShell أو Terminal في مجلد `core` وقم بتشغيل:

```bash
cd core
npm run dev
```

اترك هذا Terminal مفتوحاً.

#### الخطوة 2: تشغيل الخادم

**⚠️ مهم:** هذا المشروع مُعد بشكل خاص - يجب تشغيل الخادم من المجلد الرئيسي وليس من `core`

**الطريقة أ: استخدام ملف PowerShell (موصى به - الأسهل)**

انقر نقراً مزدوجاً على الملف: `start-server.ps1` (في المجلد الرئيسي)

أو من PowerShell في المجلد الرئيسي:

```powershell
.\start-server.ps1
```

**الطريقة ب: استخدام ملف Batch**

انقر نقراً مزدوجاً على الملف: `start-server.bat` (في المجلد الرئيسي)

**الطريقة ج: تشغيل مباشر**

افتح PowerShell في **المجلد الرئيسي** (omnimart-62nulled) وقم بتشغيل:

```bash
php -S 127.0.0.1:8000 -t . index.php
```

**ملاحظة:** لا تستخدم `php artisan serve` لأن المشروع مُعد بشكل خاص!

#### الوصول للمشروع

افتح المتصفح على: **http://127.0.0.1:8000** أو **http://localhost:8000**

---

### الطريقة 2: استخدام XAMPP/WAMP

إذا كنت تستخدم XAMPP أو WAMP:

1. ضع المشروع في مجلد `htdocs` (XAMPP) أو `www` (WAMP)
2. قم بتشغيل Apache و MySQL من لوحة التحكم
3. افتح المتصفح على: **http://localhost/omnimart-62nulled**

**ملاحظة:** في هذه الحالة، تحتاج فقط لتشغيل `npm run dev` في Terminal منفصل.

---

## 📝 ملاحظات مهمة

### قبل التشغيل:

1. **تأكد من إعداد قاعدة البيانات:**
   - افتح ملف `core/.env`
   - عدّل إعدادات قاعدة البيانات:
     ```
     DB_CONNECTION=mysql
     DB_HOST=127.0.0.1
     DB_PORT=3306
     DB_DATABASE=اسم_قاعدة_البيانات
     DB_USERNAME=اسم_المستخدم
     DB_PASSWORD=كلمة_المرور
     ```

2. **قم بتشغيل Migrations:**
   ```bash
   cd core
   php artisan migrate
   ```

3. **إنشاء رابط التخزين (إذا لزم الأمر):**
   ```bash
   php artisan storage:link
   ```

### أثناء التشغيل:

- يجب أن يكون Terminal `npm run dev` مفتوحاً دائماً
- إذا أغلقت Terminal `php artisan serve`، سيتوقف الخادم

### لإيقاف الخادم:

- اضغط `Ctrl + C` في Terminal الذي يعمل فيه `php artisan serve`

---

## 🔧 استكشاف الأخطاء

### المشكلة: "Class not found"
```bash
composer dump-autoload
```

### المشكلة: "Database connection failed"
- تحقق من إعدادات قاعدة البيانات في `.env`
- تأكد من أن MySQL يعمل
- تأكد من إنشاء قاعدة البيانات

### المشكلة: "Port 8000 already in use"
```bash
php artisan serve --port=8001
```

### المشكلة: الصور لا تظهر
```bash
php artisan storage:link
```

---

## 📞 المساعدة

إذا واجهت أي مشاكل، تحقق من:
1. ملف `core/storage/logs/laravel.log` للأخطاء
2. إعدادات قاعدة البيانات في `.env`
3. أن جميع المتطلبات مثبتة (PHP 8.1+, Composer, Node.js)

